name: CI Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  quality-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
      
      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements/dev.txt

      - name: Check Code Formatting
        run: ruff format --check .

      - name: Run Linter
        run: ruff check .

      - name: Download ML Models from Drive
        run: |
          mkdir -p models 
          gdown --id "1Si7nr4xv9p_KcLAzSdF04HGYxE4bmK7H" -O models/churn_model.onnx
          gdown --id "1hRe3Nmp_RtLjSHapadgzsKTXIjSsoFis" -O models/preprocessor.pkl

      - name: Run Unit Tests
        run: pytest
